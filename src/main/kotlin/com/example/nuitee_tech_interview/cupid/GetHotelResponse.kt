package com.example.nuitee_tech_interview.cupid

/**
 * TODO: We should limit what subset of data we would like to store within our service.
 *       Since this has not been specified by the exercise description, we will use all data as-is.
 */
data class GetHotelResponse(val data: MutableMap<String, Object>)